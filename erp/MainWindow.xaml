<Window x:Class="EduGate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EduGate.Views.Accountants"
        Title="EduGate"
        Height="700"
        Width="1200"
        FlowDirection="RightToLeft"
        Background="#F5F6FA"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Background="#1E1E2D">
            <StackPanel>
                <TextBlock Text="EduGate" Foreground="White" FontSize="22" FontWeight="SemiBold" Margin="20" HorizontalAlignment="Center"/>
                <ListBox x:Name="NavListBox" Background="Transparent" BorderThickness="0" Foreground="White" Margin="10">

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Height" Value="60"/>
                            <!-- زيادة الارتفاع -->
                            <Setter Property="Margin" Value="0,5"/>
                            <!-- مسافة بين العناصر -->
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="FontSize" Value="20"/>
                            <!-- تكبير الخط -->
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" Background="Transparent" CornerRadius="10" Padding="15">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#2D2D44"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#29293D"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <!-- ITEMS -->
                    <ListBoxItem Content="👤 المستخدمين"/>
                    <ListBoxItem Content="🧮 المحاسبين"/>
                    <ListBoxItem Content="📦 المخزون"/>
                    <ListBoxItem Content="🧾 الفواتير"/>
                    <ListBoxItem Content="🛒 الطلبات"/>
                    <ListBoxItem Content="💰 المصروفات"/>
                    <ListBoxItem Content="🏷️ الأصناف"/>
                    <ListBoxItem Content="🚚 الموردين"/>
                    <ListBoxItem Content="🔐 المصادقة"/>

                </ListBox>

            </StackPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
                <!-- TopBar للمحاسبين -->
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- TOP BAR العام -->
            <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                <TextBlock Text="لوحة التحكم" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" Margin="20,0"/>
            </Border>

            <!-- Accountant TopBar -->
            <local:AccountantsTopBar Grid.Row="1" x:Name="AccountantsTopBarControl" Visibility="Collapsed"/>

            <!-- Main Frame -->
            <Frame x:Name="MainFrame" Grid.Row="2" NavigationUIVisibility="Hidden"/>
        </Grid>

    </Grid>
</Window>
