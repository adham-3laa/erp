<Page x:Class="erp.Views.Returns.CreateReturnView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="إنشاء مرتجع"
      FlowDirection="RightToLeft">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- بيانات الطلب -->
        <Border BorderBrush="#CCC" BorderThickness="1" Padding="10" Margin="0,0,0,12">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="رقم الطلب:" Width="100"/>
                    <TextBox Width="300"
                             Text="{Binding OrderId, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="رقم العميل:" Width="100"/>
                    <TextBox Width="300"
                             Text="{Binding CustomerId, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- الأصناف -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD"
                                BorderThickness="1"
                                Padding="10"
                                Margin="0,0,0,8">
                            <StackPanel>

                                <TextBlock Text="{Binding ProductId}"
                                           FontWeight="Bold"/>

                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                    <TextBlock Text="كمية المرتجع:"
                                               Width="120"/>
                                    <TextBox Width="60"
                                             Text="{Binding ReturnQuantity,
                                             UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>

                                <TextBlock Text="سبب الإرجاع"
                                           Margin="0,6,0,2"/>

                                <TextBox Text="{Binding Reason,
                                        UpdateSourceTrigger=PropertyChanged}"
                                         Height="28"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- زر الإرسال -->
        <Button Grid.Row="2"
                Content="إرسال المرتجع"
                Width="140"
                Height="36"
                HorizontalAlignment="Left"
                Margin="0,12,0,0"
                Click="Submit_Click"/>
    </Grid>
</Page>
