<Page x:Class="erp.Views.Reports.CommissionReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      FlowDirection="RightToLeft">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Nav -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="📊 تقرير المبيعات" Click="OpenSalesReport" Margin="0,0,8,0"/>
            <Button Content="📦 حركة صنف" Click="OpenStockMovementReport" Margin="0,0,8,0"/>
            <Button Content="💰 عمولات المندوبين" Background="#1E1B4B" Foreground="White"/>
        </StackPanel>

        <!-- Input -->
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox Width="300"
                     Text="{Binding SalesRepId}"
                     materialDesign:HintAssist.Hint="أدخل SalesRep ID"/>
            <Button Content="تحميل التقرير"
                    Command="{Binding LoadReportCommand}"
                    Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Table -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="True"/>
    </Grid>
</Page>
