<Page x:Class="erp.Views.Reports.SalesRepReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      FlowDirection="RightToLeft"
      Background="{StaticResource PageBgBrush}">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <Style x:Key="ActiveTabButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,10"/>
        </Style>

        <Style x:Key="InactiveTabButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="18" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4B5563"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SummaryCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="👔 تقرير مناديب المبيعات" FontSize="26" FontWeight="Bold" Foreground="#FF1F2937"/>
                <TextBlock Text="تقرير شامل عن أداء وعمولات مندوب مبيعات" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="📊 المبيعات" Style="{StaticResource InactiveTabButtonStyle}" Margin="0,0,8,0" Click="OpenSalesReport"/>
                <Button Content="📦 حركة صنف" Style="{StaticResource InactiveTabButtonStyle}" Margin="0,0,8,0" Click="OpenStockMovementReport"/>
                <Button Content="💰 العمولات" Style="{StaticResource InactiveTabButtonStyle}" Margin="0,0,8,0" Click="OpenCommissionReport"/>
                <Button Content="👤 العملاء" Style="{StaticResource InactiveTabButtonStyle}" Margin="0,0,8,0" Click="OpenCustomerReport"/>
                <Button Content="👔 المندوبين" Style="{StaticResource ActiveTabButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="🏭 الموردين" Style="{StaticResource InactiveTabButtonStyle}" Click="OpenSupplierReport"/>
            </StackPanel>
        </Grid>

        <!-- Search -->
        <Border Grid.Row="1" Margin="0,20,0,0" Padding="16" Background="White" CornerRadius="16" BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AccountSearch" VerticalAlignment="Center" Foreground="Gray" Width="24" Height="24" Margin="0,0,8,0"/>
                <TextBox Width="400" materialDesign:HintAssist.Hint="اسم المندوب" Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding SalesRepName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                <Button Content="بحث" Command="{Binding LoadReportCommand}" Style="{StaticResource DarkButtonStyle}" Width="120" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="2" Margin="0,20,0,0" VerticalScrollBarVisibility="Auto">
            <Grid Visibility="{Binding HasData, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Summary Cards -->
                <UniformGrid Grid.Row="0" Columns="3">
                    <Border Style="{StaticResource SummaryCard}" Background="#EBF5FF">
                        <StackPanel>
                            <TextBlock Text="حجم المبيعات" Foreground="#1E40AF" FontSize="12"/>
                            <TextBlock Text="{Binding Report.TotalSalesVolume, StringFormat='{}{0:N0} ج.م'}" Foreground="#1E3A8A" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SummaryCard}" Background="#ECFDF5">
                        <StackPanel>
                            <TextBlock Text="إجمالي العمولات المستحقة" Foreground="#065F46" FontSize="12"/>
                            <TextBlock Text="{Binding Report.TotalCommissionEarned, StringFormat='{}{0:N0} ج.م'}" Foreground="#064E3B" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SummaryCard}" Background="#FFFBEB">
                        <StackPanel>
                            <TextBlock Text="العمولات المتبقية" Foreground="#92400E" FontSize="12"/>
                            <TextBlock Text="{Binding Report.TotalCommissionDue, StringFormat='{}{0:N0} ج.م'}" Foreground="#78350F" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <UniformGrid Grid.Row="1" Columns="3" Margin="0,10,0,0">
                    <Border Style="{StaticResource SummaryCard}" Background="#F3F4F6">
                         <StackPanel>
                            <TextBlock Text="عدد الطلبات" Foreground="#374151" FontSize="12"/>
                            <TextBlock Text="{Binding Report.TotalOrdersCount}" Foreground="#111827" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SummaryCard}" Background="#F3F4F6">
                        <StackPanel>
                            <TextBlock Text="عدد المرتجعات" Foreground="#374151" FontSize="12"/>
                            <TextBlock Text="{Binding Report.TotalReturnsCount}" Foreground="#111827" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource SummaryCard}" Background="#F3F4F6">
                        <StackPanel>
                            <TextBlock Text="حجم المرتجعات" Foreground="#374151" FontSize="12"/>
                            <TextBlock Text="{Binding Report.TotalReturnsVolume, StringFormat='{}{0:N0}'}" Foreground="#111827" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Unpaid Commissions Table -->
                <materialDesign:Card Grid.Row="2" Margin="4,20,4,4" Padding="16" materialDesign:ElevationAssist.Elevation="Dp1">
                    <StackPanel>
                        <TextBlock Text="💵 عمولات غير مدفوعة" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                        <DataGrid ItemsSource="{Binding Report.UnpaidCommissions}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" MaxHeight="400">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="كود الفاتورة" Binding="{Binding InvoiceCode}" Width="Auto"/>
                                <DataGridTextColumn Header="المبلغ المستحق" Binding="{Binding OriginalAmount, StringFormat='N0'}" Width="*"/>
                                <DataGridTextColumn Header="المتبقي للدافع" Binding="{Binding RemainingAmount, StringFormat='N0'}" Width="*"/>
                                <DataGridTextColumn Header="التاريخ" Binding="{Binding InvoiceDate, StringFormat='yyyy/MM/dd'}" Width="Auto"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
        </ScrollViewer>

        <!-- Loading -->
        <Border Grid.Row="2" Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="6"/>
                <TextBlock Text="جاري تحميل التقرير..." Margin="0,12,0,0" Foreground="#374151" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- No Data -->
        <TextBlock Grid.Row="2" Text="لا توجد بيانات لهذا المندوب" Visibility="{Binding NoData, Converter={StaticResource BoolToVisibilityConverter}}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" Foreground="Gray"/>
    </Grid>
</Page>
