<Page x:Class="erp.Views.Reports.DualRoleReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:converters="clr-namespace:erp.Converters"
      FlowDirection="RightToLeft"
      Background="{StaticResource PageBgBrush}">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:DateToDaysAgoConverter x:Key="DateToDaysAgoConverter"/>
        <converters:ArabicDateConverter x:Key="ArabicDateConverter"/>
        <converters:InvoiceCountConverter x:Key="InvoiceCountConverter"/>
        <converters:ReturnsCountConverter x:Key="ReturnsCountConverter"/>
        <converters:DateToAgeColorConverter x:Key="DateToAgeColorConverter"/>
        <converters:BoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" Invert="True"/>
        <converters:NetBalanceTextConverter x:Key="NetBalanceTextConverter"/>
        <converters:NetBalanceColorConverter x:Key="NetBalanceColorConverter"/>
        
        <Style x:Key="ActiveTabButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,10"/>
        </Style>

        <Style x:Key="InactiveTabButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="18" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4B5563"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SummaryCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeroCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Padding" Value="28"/>
            <Setter Property="Margin" Value="0,12,0,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="☯️ تقرير مشترك (مورد / عميل)" FontSize="26" FontWeight="Bold" Foreground="#FF1F2937"/>
                <TextBlock Text="تحليل شامل للمعاملات المتبادلة كعميل وكمورد" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button Content="📊 المبيعات" Style="{StaticResource InactiveTabButtonStyle}" Margin="8,0,0,0" Click="OpenSalesReport"/>
                <Button Content="📦 حركة صنف" Style="{StaticResource InactiveTabButtonStyle}" Margin="8,0,0,0" Click="OpenStockMovementReport"/>
                <!-- <Button Content="💰 العمولات" Style="{StaticResource InactiveTabButtonStyle}" Margin="8,0,0,0" Click="OpenCommissionReport"/> -->
                <Button Content="👤 العملاء" Style="{StaticResource InactiveTabButtonStyle}" Margin="8,0,0,0" Click="OpenCustomerReport"/>
                <!-- <Button Content="👔 المندوبين" Style="{StaticResource InactiveTabButtonStyle}" Margin="8,0,0,0" Click="OpenSalesRepReport"/> -->
                <Button Content="🏭 الموردين" Style="{StaticResource InactiveTabButtonStyle}" Margin="8,0,0,0" Click="OpenSupplierReport"/>
                <Button Content="☯️ مورد / عميل" Style="{StaticResource ActiveTabButtonStyle}" Margin="8,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Search Bar -->
        <Border Grid.Row="1" Margin="0,20,0,0" Padding="16" Background="White" CornerRadius="16" BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <materialDesign:PackIcon Grid.Column="0" Kind="AccountSwitch" VerticalAlignment="Center" Foreground="Gray" Width="24" Height="24" Margin="0,0,12,0"/>
                
                <!-- Autocomplete ComboBox -->
                <Grid Grid.Column="1" Width="400" HorizontalAlignment="Right">
                    <ComboBox IsEditable="True"
                              StaysOpenOnEdit="True"
                              ItemsSource="{Binding Suggestions}"
                              Text="{Binding SearchName, UpdateSourceTrigger=PropertyChanged}"
                              materialDesign:HintAssist.Hint="اسم الطرف (مورد / عميل)"
                              VerticalAlignment="Center">
                        <ComboBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding LoadReportCommand}"/>
                        </ComboBox.InputBindings>
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>
                    <ProgressBar IsIndeterminate="True" 
                                 Width="20" Height="20" 
                                 HorizontalAlignment="Right" 
                                 VerticalAlignment="Bottom"
                                 Margin="0,0,5,5"
                                 Visibility="{Binding IsSuppliersLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Style="{StaticResource MaterialDesignCircularProgressBar}"/>
                </Grid>
                
                <Button Grid.Column="2" 
                        Content="بحث" 
                        Command="{Binding LoadReportCommand}" 
                        Style="{StaticResource DarkButtonStyle}" 
                        Width="120" 
                        Margin="16,0"/>
            </Grid>
        </Border>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="2" Margin="0,20,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Error Banner -->
                <Border Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}" 
                        Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Background="#FEE2E2" CornerRadius="12" Width="48" Height="48" Margin="0,0,16,0" VerticalAlignment="Top">
                             <materialDesign:PackIcon Kind="{Binding ErrorState.IconKind}" Foreground="#DC2626" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ErrorState.Title}" FontWeight="Bold" FontSize="16" Foreground="#991B1B" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding ErrorState.Message}" FontSize="14" Foreground="#B91C1C" TextWrapping="Wrap" Opacity="0.9"/>
                        </StackPanel>

                        <Button Grid.Column="2" 
                                Content="{Binding ErrorState.ActionText}" 
                                Command="{Binding LoadReportCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Foreground="#DC2626" BorderBrush="#DC2626"
                                Margin="16,0,0,0" VerticalAlignment="Center"
                                Height="36" Padding="16,0"/>
                    </Grid>
                </Border>

                <!-- Initial State -->
                <Border Visibility="{Binding ShowInitialState, Converter={StaticResource BoolToVisibilityConverter}}" 
                        Background="#F9FAFB" CornerRadius="16" Padding="60" Margin="0,40,0,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="☯️" FontSize="72" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        <TextBlock Text="ابحث عن الاسم لعرض التقرير المشترك" FontSize="20" FontWeight="SemiBold" Foreground="#374151" HorizontalAlignment="Center"/>
                        <TextBlock Text="أدخل الاسم في الأعلى وانقر على زر 'بحث'" FontSize="14" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Data Content -->
                <StackPanel Visibility="{Binding HasData, Converter={StaticResource BoolToVisibilityConverter}}">
                    
                    <!-- User Info Card -->
                    <Border Background="White" Style="{StaticResource SummaryCard}" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <materialDesign:PackIcon Grid.Column="0" Kind="AccountSwitch" Width="40" Height="40" Foreground="#6366F1" VerticalAlignment="Center" Margin="0,0,20,0"/>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Report.FullName}" FontSize="20" FontWeight="Bold" Foreground="#1F2937"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <materialDesign:PackIcon Kind="BadgeAccount" Width="16" Height="16" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="رقم الملف:" FontSize="13" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="{Binding Report.UserNumber, StringFormat='#{0}'}" FontSize="13" Foreground="#6B7280" VerticalAlignment="Center"/>
                                    
                                    <TextBlock Text="•" FontSize="14" Foreground="#D1D5DB" Margin="12,0"/>
                                    <materialDesign:PackIcon Kind="Phone" Width="16" Height="16" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock Text="{Binding Report.PhoneNumber}" FontSize="13" Foreground="#6B7280" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Hero Metric - Net Balance -->
                    <Border Style="{StaticResource HeroCard}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#0F172A" Offset="0"/>
                                <GradientStop Color="#1E293B" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Report.BalanceStatus}" FontSize="18" Foreground="White" 
                                      HorizontalAlignment="Center" Opacity="0.95" TextWrapping="Wrap" TextAlignment="Center" FontWeight="SemiBold"/>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <!-- Amount -->
                                        <TextBlock Text="{Binding Report.NetBalance, StringFormat='{}{0:N0}'}" 
                                              FontSize="44" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                        <TextBlock Text=" ج.م" FontSize="24" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                                        
                                        <!-- Action Label (Pay/Collect) -->
                                        <Border Background="#334155" CornerRadius="8" Padding="12,4" Margin="16,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Report.NetBalance, Converter={StaticResource NetBalanceTextConverter}}" 
                                                  FontSize="20" FontWeight="Bold" 
                                                  Foreground="{Binding Report.NetBalance, Converter={StaticResource NetBalanceColorConverter}}"/>
                                        </Border>
                                    </StackPanel>
                            </StackPanel>
                            
                            <!-- Breakdown under Hero -->
                            <Grid Margin="0,24,0,0" HorizontalAlignment="Center" Width="400">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="لنا (من العملاء)" FontSize="12" Foreground="#93C5FD" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Report.TotalMoneyForUs, StringFormat='{}{0:N0}'}" FontSize="18" FontWeight="Bold" Foreground="#60A5FA" HorizontalAlignment="Center"/>
                                </StackPanel>
                                
                                <Rectangle Grid.Column="1" Width="1" Fill="#334155" Margin="20,0"/>

                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="له (من الموردين)" FontSize="12" Foreground="#FDBA74" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Report.TotalMoneyForHim, StringFormat='{}{0:N0}'}" FontSize="18" FontWeight="Bold" Foreground="#FB923C" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>


                    <!-- ================= SECTION 1: CUSTOMER ACTIVITY ================= -->
                    <Border Background="White" CornerRadius="16" BorderBrush="#E5E7EB" BorderThickness="1" Margin="0,32,0,0" Padding="24">
                        <StackPanel>
                            <!-- Header -->
                            <Border Background="#EFF6FF" CornerRadius="12" Padding="16" Margin="0,0,0,24" BorderBrush="#BFDBFE" BorderThickness="1">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                                        <materialDesign:PackIcon Kind="AccountTie" Margin="0,0,12,0" Foreground="#1E40AF" Width="28" Height="28"/>
                                        <TextBlock Text="نشاط كعميل (لنا)" FontWeight="Bold" Foreground="#1E40AF" FontSize="20"/>
                                    </StackPanel>
                                    <TextBlock Text="تفاصيل المبيعات - الإيرادات - الفواتير المستحقة" HorizontalAlignment="Center" FontSize="14" Foreground="#3B82F6"/>
                                </StackPanel>
                            </Border>

                            <!-- Metrics Grid -->
                            <Grid Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#F0FDFA" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="إجمالي المبيعات" FontSize="11" FontWeight="SemiBold" Foreground="#0F766E"/>
                                        <TextBlock Text="{Binding Report.CustomerActivity.TotalSalesAmount, StringFormat='N0'}" FontWeight="Bold" Foreground="#0D9488" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Background="#FEF2F2" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="المتبقي (ديون)" FontSize="11" FontWeight="SemiBold" Foreground="#991B1B"/>
                                        <TextBlock Text="{Binding Report.CustomerActivity.TotalDebt, StringFormat='N0'}" FontWeight="Bold" Foreground="#DC2626" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Background="#F0F9FF" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="إجمالي المدفوع" FontSize="11" FontWeight="SemiBold" Foreground="#0369A1"/>
                                        <TextBlock Text="{Binding Report.CustomerActivity.TotalPaid, StringFormat='N0'}" FontWeight="Bold" Foreground="#0284C7" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="3" Background="#EFF6FF" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="عدد الطلبات" FontSize="11" FontWeight="SemiBold" Foreground="#1E3A8A"/>
                                        <TextBlock Text="{Binding Report.CustomerActivity.TotalOrdersCount}" FontWeight="Bold" Foreground="#1E40AF" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="4" Background="#FFFBEB" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="عدد المرتجعات" FontSize="11" FontWeight="SemiBold" Foreground="#92400E"/>
                                        <TextBlock Text="{Binding Report.CustomerActivity.TotalReturnsCount}" FontWeight="Bold" Foreground="#B45309" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="5" Background="#F3F4F6" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="آخر طلب" FontSize="11" FontWeight="SemiBold" Foreground="#4B5563"/>
                                        <TextBlock Text="{Binding Report.CustomerActivity.LastOrderDate, Converter={StaticResource DateToDaysAgoConverter}}" FontWeight="Bold" Foreground="#374151" FontSize="14" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Tables Area (Side by Side) -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Unpaid Invoices Table -->
                                <materialDesign:Card Grid.Column="0" Padding="0" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8">
                                    <StackPanel>
                                        <Border Background="#F9FAFB" Padding="16" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🧾 فواتير بيع غير مدفوعة" FontWeight="Bold" FontSize="14"/>
                                                <Border Background="#DBEAFE" CornerRadius="8" Padding="8,2" Margin="12,0,0,0">
                                                    <TextBlock Text="{Binding Report.CustomerActivity.UnpaidInvoices.Count}" FontSize="11" FontWeight="Bold" Foreground="#1E40AF"/>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                        <DataGrid ItemsSource="{Binding Report.CustomerActivity.UnpaidInvoices}" 
                                                  AutoGenerateColumns="False" 
                                                  CanUserAddRows="False" 
                                                  IsReadOnly="True" 
                                                  MaxHeight="400"
                                                  HeadersVisibility="Column"
                                                  GridLinesVisibility="Horizontal"
                                                  HorizontalGridLinesBrush="#F3F4F6">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="#" Binding="{Binding InvoiceCode}" Width="Auto" FontWeight="SemiBold"/>
                                                <DataGridTextColumn Header="المبلغ" Binding="{Binding OriginalAmount, StringFormat='N0'}" Width="*"/>
                                                <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingAmount, StringFormat='N0'}" Width="*">
                                                    <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#DC2626"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="التاريخ" Binding="{Binding InvoiceDate, Converter={StaticResource ArabicDateConverter}}" Width="Auto" FontSize="12"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>
                                </materialDesign:Card>

                                <!-- Unpaid Returns Table -->
                                <materialDesign:Card Grid.Column="2" Padding="0" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8">
                                    <StackPanel>
                                        <Border Background="#F9FAFB" Padding="16" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="↩️ مرتجعات بيع (لنا)" FontWeight="Bold" FontSize="14"/>
                                                <Border Background="#FEF3C7" CornerRadius="8" Padding="8,2" Margin="12,0,0,0">
                                                    <TextBlock Text="{Binding Report.CustomerActivity.UnpaidReturns.Count}" FontSize="11" FontWeight="Bold" Foreground="#92400E"/>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                        <DataGrid ItemsSource="{Binding Report.CustomerActivity.UnpaidReturns}" 
                                                  AutoGenerateColumns="False" 
                                                  CanUserAddRows="False" 
                                                  IsReadOnly="True" 
                                                  MaxHeight="400"
                                                  HeadersVisibility="Column"
                                                  GridLinesVisibility="Horizontal"
                                                  HorizontalGridLinesBrush="#F3F4F6">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="#" Binding="{Binding InvoiceCode}" Width="Auto" FontWeight="SemiBold"/>
                                                <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingAmount, StringFormat='N0'}" Width="*">
                                                     <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D97706"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="التاريخ" Binding="{Binding InvoiceDate, Converter={StaticResource ArabicDateConverter}}" Width="Auto" FontSize="12"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>
                                </materialDesign:Card>
                            </Grid>
                        </StackPanel>
                    </Border>


                    <!-- ================= SECTION 2: SUPPLIER ACTIVITY ================= -->
                    <Border Background="White" CornerRadius="16" BorderBrush="#E5E7EB" BorderThickness="1" Margin="0,32,0,40" Padding="24">
                        <StackPanel>
                            <!-- Header -->
                            <Border Background="#FFF7ED" CornerRadius="12" Padding="16" Margin="0,0,0,24" BorderBrush="#FFEDD5" BorderThickness="1">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                                        <materialDesign:PackIcon Kind="Truck" Margin="0,0,12,0" Foreground="#EA580C" Width="28" Height="28"/>
                                        <TextBlock Text="نشاط كمورد (له)" FontWeight="Bold" Foreground="#EA580C" FontSize="20"/>
                                    </StackPanel>
                                    <TextBlock Text="تفاصيل التوريد - المشتريات - المديونيات" HorizontalAlignment="Center" FontSize="14" Foreground="#F97316"/>
                                </StackPanel>
                            </Border>

                            <!-- Metrics Grid -->
                            <Grid Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#F0F9FF" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="إجمالي التوريد" FontSize="11" FontWeight="SemiBold" Foreground="#0369A1"/>
                                        <TextBlock Text="{Binding Report.SupplierActivity.TotalSupplyAmount, StringFormat='N0'}" FontWeight="Bold" Foreground="#0284C7" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Background="#FFFBEB" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="المتبقي (له)" FontSize="11" FontWeight="SemiBold" Foreground="#B45309"/>
                                        <TextBlock Text="{Binding Report.SupplierActivity.TotalDebt, StringFormat='N0'}" FontWeight="Bold" Foreground="#D97706" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Background="#ECFDF5" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="إجمالي المدفوع" FontSize="11" FontWeight="SemiBold" Foreground="#047857"/>
                                        <TextBlock Text="{Binding Report.SupplierActivity.TotalPaid, StringFormat='N0'}" FontWeight="Bold" Foreground="#059669" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="3" Background="#EEF2FF" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="عدد التوريدات" FontSize="11" FontWeight="SemiBold" Foreground="#3730A3"/>
                                        <TextBlock Text="{Binding Report.SupplierActivity.TotalSupplyCount}" FontWeight="Bold" Foreground="#4338CA" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="4" Background="#FAF5FF" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="عدد المرتجعات" FontSize="11" FontWeight="SemiBold" Foreground="#6B21A8"/>
                                        <TextBlock Text="{Binding Report.SupplierActivity.TotalReturnsCount}" FontWeight="Bold" Foreground="#7E22CE" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="5" Background="#FEF2F2" CornerRadius="8" Padding="12" Margin="4">
                                    <StackPanel>
                                        <TextBlock Text="صافي الدين (له)" FontSize="11" FontWeight="SemiBold" Foreground="#991B1B"/>
                                        <TextBlock Text="{Binding Report.SupplierActivity.TotalNetDebt, StringFormat='N0'}" FontWeight="Bold" Foreground="#B91C1C" FontSize="16" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            
                            <!-- Tables Area (Side by Side) -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Unpaid Invoices Table -->
                                <materialDesign:Card Grid.Column="0" Padding="0" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8">
                                    <StackPanel>
                                        <Border Background="#F9FAFB" Padding="16" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🧾 فواتير شراء غير مدفوعة" FontWeight="Bold" FontSize="14"/>
                                                <Border Background="#FFE4E6" CornerRadius="8" Padding="8,2" Margin="12,0,0,0">
                                                    <TextBlock Text="{Binding Report.SupplierActivity.UnpaidInvoices.Count}" FontSize="11" FontWeight="Bold" Foreground="#BE123C"/>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                        <DataGrid ItemsSource="{Binding Report.SupplierActivity.UnpaidInvoices}" 
                                                  AutoGenerateColumns="False" 
                                                  CanUserAddRows="False" 
                                                  IsReadOnly="True" 
                                                  MaxHeight="400"
                                                  HeadersVisibility="Column"
                                                  GridLinesVisibility="Horizontal"
                                                  HorizontalGridLinesBrush="#F3F4F6">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="#" Binding="{Binding InvoiceCode}" Width="Auto" FontWeight="SemiBold"/>
                                                <DataGridTextColumn Header="المبلغ" Binding="{Binding OriginalAmount, StringFormat='N0'}" Width="*"/>
                                                <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingAmount, StringFormat='N0'}" Width="*">
                                                    <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#BE123C"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="التاريخ" Binding="{Binding InvoiceDate, Converter={StaticResource ArabicDateConverter}}" Width="Auto" FontSize="12"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>
                                </materialDesign:Card>

                                <!-- Unpaid Returns Table -->
                                <materialDesign:Card Grid.Column="2" Padding="0" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8">
                                    <StackPanel>
                                        <Border Background="#F9FAFB" Padding="16" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="↩️ مرتجعات شراء (له)" FontWeight="Bold" FontSize="14"/>
                                                <Border Background="#F3E8FF" CornerRadius="8" Padding="8,2" Margin="12,0,0,0">
                                                    <TextBlock Text="{Binding Report.SupplierActivity.UnpaidReturns.Count}" FontSize="11" FontWeight="Bold" Foreground="#7E22CE"/>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                        <DataGrid ItemsSource="{Binding Report.SupplierActivity.UnpaidReturns}" 
                                                  AutoGenerateColumns="False" 
                                                  CanUserAddRows="False" 
                                                  IsReadOnly="True" 
                                                  MaxHeight="400"
                                                  HeadersVisibility="Column"
                                                  GridLinesVisibility="Horizontal"
                                                  HorizontalGridLinesBrush="#F3F4F6">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="#" Binding="{Binding InvoiceCode}" Width="Auto" FontWeight="SemiBold"/>
                                                <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingAmount, StringFormat='N0'}" Width="*">
                                                     <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#7E22CE"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="التاريخ" Binding="{Binding InvoiceDate, Converter={StaticResource ArabicDateConverter}}" Width="Auto" FontSize="12"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </StackPanel>
                                </materialDesign:Card>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- No Data State (Bottom) -->
                <Border Visibility="{Binding NoData, Converter={StaticResource BoolToVisibilityConverter}}" 
                        Background="#FEF9C3" BorderBrush="#FDE047" BorderThickness="1" CornerRadius="16" Padding="60" Margin="0,40,0,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🔍" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        <TextBlock Text="لم يتم العثور على نتائج" FontSize="20" FontWeight="SemiBold" Foreground="#374151" HorizontalAlignment="Center"/>
                        <TextBlock Text="تأكد من اختيار مورد/عميل صحيح" FontSize="14" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Border Grid.Row="2" Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="6" Style="{StaticResource MaterialDesignCircularProgressBar}"/>
                <TextBlock Text="جاري تحميل التقرير المشترك..." Margin="0,16,0,0" Foreground="#374151" FontSize="14" FontWeight="Medium" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
