<Page x:Class="erp.Views.Reports.StockMovementReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      FlowDirection="RightToLeft">

    <!-- ===== Resources ===== -->
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Base Button -->
        <Style x:Key="SimpleButton" TargetType="Button">
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active Tab -->
        <Style x:Key="ActiveTabButton"
               TargetType="Button"
               BasedOn="{StaticResource SimpleButton}">
            <Setter Property="Background" Value="#1E1B4B"/>
        </Style>
    </Page.Resources>

    <!-- ===== Layout ===== -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== Header ===== -->
        <StackPanel Margin="0,0,0,16">
            <TextBlock Text="📦 تقرير حركة صنف"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="#111827"/>
            <TextBlock Text="متابعة حركة الإدخال والإخراج للمخزون"
                       FontSize="14"
                       Foreground="#6B7280"/>
        </StackPanel>

        <!-- ===== Top Navigation ===== -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,8,0,24">

            <Button Content="📊 تقرير المبيعات"
                    Style="{StaticResource SimpleButton}"
                    Click="OpenSalesReport"
                    Margin="0,0,8,0"/>

            <Button Content="📦 حركة صنف"
                    Style="{StaticResource ActiveTabButton}"
                    Margin="0,0,8,0"/>

            <Button Content="💰 عمولات المندوبين"
                    Style="{StaticResource SimpleButton}"
                    Click="OpenCommissionReport"/>
        </StackPanel>

        <!-- ===== Input Card ===== -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="14"
                BorderThickness="1"
                BorderBrush="#E5E7EB"
                Padding="16"
                Margin="0,0,0,24">

            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">

                <StackPanel>
                    <TextBlock Text="معرّف الصنف"
                               FontSize="12"
                               Foreground="#6B7280"/>
                    <TextBox Width="320"
                             Height="36"
                             Text="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="أدخل Product ID"/>
                </StackPanel>

                <Button Content="تحميل التقرير"
                        Command="{Binding LoadReportCommand}"
                        Style="{StaticResource ActiveTabButton}"
                        Height="36"
                        Margin="12,18,0,0"/>
            </StackPanel>
        </Border>

        <!-- ===== Result Cards ===== -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Total In -->
            <Border Grid.Column="0"
                    Margin="8"
                    Padding="24"
                    CornerRadius="16"
                    Background="#2563EB">
                <StackPanel>
                    <TextBlock Text="إجمالي الداخل"
                               Foreground="#DBEAFE"
                               FontSize="14"/>
                    <TextBlock Text="{Binding Report.TotalIn}"
                               Foreground="White"
                               FontSize="28"
                               FontWeight="Bold"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Total Out -->
            <Border Grid.Column="1"
                    Margin="8"
                    Padding="24"
                    CornerRadius="16"
                    Background="#DC2626">
                <StackPanel>
                    <TextBlock Text="إجمالي الخارج"
                               Foreground="#FEE2E2"
                               FontSize="14"/>
                    <TextBlock Text="{Binding Report.TotalOut}"
                               Foreground="White"
                               FontSize="28"
                               FontWeight="Bold"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Current Stock -->
            <Border Grid.Column="2"
                    Margin="8"
                    Padding="24"
                    CornerRadius="16"
                    Background="#10B981">
                <StackPanel>
                    <TextBlock Text="الرصيد الحالي"
                               Foreground="#D1FAE5"
                               FontSize="14"/>
                    <TextBlock Text="{Binding Report.CurrentStock}"
                               Foreground="White"
                               FontSize="28"
                               FontWeight="Bold"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
