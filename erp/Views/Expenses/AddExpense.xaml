<Page x:Class="erp.Views.Expenses.AddExpensePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource PageBgBrush}"
      FlowDirection="RightToLeft">

    <Page.Resources>

        <!-- ===== Glow (needed for our old frame) ===== -->
        <DropShadowEffect x:Key="PrimaryOutlineGlow"
                          Color="{Binding Color, Source={StaticResource PrimaryBrush}}"
                          BlurRadius="16"
                          ShadowDepth="0"
                          Opacity="0.35"/>

        <!-- ===== Old Frame (our design) ===== -->
        <Style x:Key="GlowingTableOuterBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Effect" Value="{StaticResource PrimaryOutlineGlow}"/>
        </Style>

    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- ================= Header (Centered) ================= -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="إضافة مصروف جديد"
               FontSize="26"
               FontWeight="Bold"
               Foreground="{StaticResource TextDarkBrush}"
               HorizontalAlignment="Center"
               TextAlignment="Center"/>
        </Grid>


        <!-- ================= Form (Centered + Our Frame) ================= -->
        <Border Grid.Row="1"
                Margin="0,0,0,24"
                Style="{StaticResource GlowingTableOuterBorder}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <!-- Inner Card (keep your clean design) -->
            <Border Style="{StaticResource InnerCardContainer}"
                    Width="620"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0">

                <StackPanel>

                    <!-- ================= Amount ================= -->
                    <TextBlock Text="المبلغ"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextMutedBrush}"
                               Margin="0,0,0,6"/>

                    <Border Style="{StaticResource GlowInputFrame}">
                        <Border Style="{StaticResource GlowInputInner}">
                            <TextBox Style="{StaticResource GlowTextBox}"
                                     Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"/>
                        </Border>
                    </Border>

                    <!-- ================= Description ================= -->
                    <TextBlock Text="الوصف"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextMutedBrush}"
                               Margin="0,20,0,6"/>

                    <Border Style="{StaticResource GlowInputFrame}">
                        <Border Style="{StaticResource GlowInputInner}">
                            <TextBox Style="{StaticResource GlowTextBox}"
                                     Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     Height="100"/>
                        </Border>
                    </Border>

                    <!-- ================= Messages (keep same logic) ================= -->
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="#DC2626"
                               Margin="0,14,0,0"
                               Visibility="{Binding ErrorMessage,
                                   Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>

                    <TextBlock Text="تم إضافة المصروف بنجاح ✔"
                               Foreground="#16A34A"
                               Margin="0,14,0,0"
                               Visibility="{Binding IsSuccess,
                                   Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <!-- ================= Actions ================= -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Left"
                                Margin="0,32,0,0">

                        <Button Content="حفظ المصروف"
                                Width="160"
                                Margin="0,0,12,0"
                                Style="{StaticResource DarkButtonStyle}"
                                Command="{Binding SaveCommand}"
                                IsEnabled="{Binding IsBusy,
                                    Converter={StaticResource BoolToVisibilityConverter},
                                    ConverterParameter=invert}"/>

                        <Button Content="رجوع"
                                Width="120"
                                Style="{StaticResource DarkButtonStyle}"
                                Click="Back_Click"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Page>
