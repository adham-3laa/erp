<Page x:Class="erp.Views.Cheques.ChequesListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:erp.Views.Cheques"
      xmlns:converters="clr-namespace:erp.Converters"
      mc:Ignorable="d"
      Title="ChequesListPage"
      FlowDirection="RightToLeft"
      Background="#F3F4F6">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BooleanToTextConverter x:Key="BooleanToTextConverter"/>
        <converters:ChequeStatusToColorConverter x:Key="ChequeStatusToColorConverter"/>
        <converters:ChequeDirectionConverter x:Key="ChequeDirectionConverter"/>
        <converters:OverdueToColorConverter x:Key="OverdueToColorConverter"/>
        <converters:CollectionCountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <converters:ChequeActionTooltipConverter x:Key="ChequeActionTooltipConverter"/>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Filters -->
            <RowDefinition Height="*"/>    <!-- Content Area -->
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Add Cheque Button - Left Side (appears on right in RTL) -->
            <Button Grid.Column="0" 
                    Content="+ إضافة شيك جديد"
                    Background="#2563EB"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Padding="20,10"
                    VerticalAlignment="Center"
                    Click="AddCheque_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <!-- Title - Right Side (appears on left in RTL) -->
            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="20,0,0,0">
                <TextBlock Text="سجل الشيكات" FontSize="32" FontWeight="Bold" Foreground="#1F2937"/>
                <TextBlock Text="تتبع الحركة المالية والشيكات الصادرة والواردة" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Dynamic Filter Panel -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,24" BorderBrush="#F3F4F6" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.05"/>
            </Border.Effect>
            <WrapPanel Orientation="Horizontal">
                <!-- Search -->
                <StackPanel Margin="0,0,20,10">
                    <TextBlock Text="الطرف الآخر (عميل/مورد)" FontSize="12" FontWeight="Medium" Foreground="#374151" Margin="0,0,0,6"/>
                    <TextBox Width="220" Height="36" Padding="8,4" VerticalContentAlignment="Center"
                             Text="{Binding SearchName, UpdateSourceTrigger=PropertyChanged}" 
                             Tag="Search here..."/>
                </StackPanel>

                <!-- Type -->
                <StackPanel Margin="0,0,20,10">
                    <TextBlock Text="نوع الحركة" FontSize="12" FontWeight="Medium" Foreground="#374151" Margin="0,0,0,6"/>
                    <ComboBox Width="160" Height="36" SelectedValuePath="Tag" 
                              SelectedValue="{Binding SelectedDirection, UpdateSourceTrigger=PropertyChanged}">
                        <ComboBoxItem Content="الكل" Tag=""/>
                        <ComboBoxItem Content="وارد" Tag="Incoming"/>
                        <ComboBoxItem Content="صادر" Tag="Outgoing"/>
                    </ComboBox>
                </StackPanel>

                <!-- Status -->
                <StackPanel Margin="0,0,20,10">
                    <TextBlock Text="الحالة الحالية" FontSize="12" FontWeight="Medium" Foreground="#374151" Margin="0,0,0,6"/>
                    <ComboBox Width="160" Height="36" SelectedValuePath="Tag" 
                              SelectedValue="{Binding SelectedStatus, UpdateSourceTrigger=PropertyChanged}">
                        <ComboBoxItem Content="الكل" Tag=""/>
                        <ComboBoxItem Content="قيد الانتظار" Tag="Pending"/>
                        <ComboBoxItem Content="تم التحصيل" Tag="Collected"/>
                        <ComboBoxItem Content="تم الدفع" Tag="Paid"/>
                        <ComboBoxItem Content="مرفوض / مرتد" Tag="Rejected"/>
                        <ComboBoxItem Content="ملغى" Tag="Cancelled"/>
                    </ComboBox>
                </StackPanel>

                <!-- From Date -->
                <StackPanel Margin="0,0,20,10">
                    <TextBlock Text="من تاريخ" FontSize="12" FontWeight="Medium" Foreground="#374151" Margin="0,0,0,6"/>
                    <DatePicker Width="140" Height="36" SelectedDate="{Binding StartDate}"/>
                </StackPanel>

                <!-- To Date -->
                <StackPanel Margin="0,0,20,10">
                    <TextBlock Text="إلى تاريخ" FontSize="12" FontWeight="Medium" Foreground="#374151" Margin="0,0,0,6"/>
                    <DatePicker Width="140" Height="36" SelectedDate="{Binding EndDate}"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <!-- Loading Indicator -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" 
                        Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="10">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#2563EB"/>
                <TextBlock Text="جاري تحميل البيانات..." Margin="0,10" HorizontalAlignment="Center" Foreground="#6B7280"/>
            </StackPanel>

            <!-- Empty State -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                        Visibility="{Binding Cheques.Count, Converter={StaticResource CountToVisibilityConverter}}">
                <TextBlock Text="📒" FontSize="64" HorizontalAlignment="Center"/>
                <TextBlock Text="لا توجد شيكات تطابق البحث" FontSize="18" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,10"/>
                <TextBlock Text="حاول تغيير معايير البحث أو إضافة شيك جديد" Foreground="#9CA3AF"/>
            </StackPanel>

            <!-- Modern DataGrid -->
            <Border Background="White" CornerRadius="12" BorderBrush="#F3F4F6" BorderThickness="1" ClipToBounds="True">
                <DataGrid ItemsSource="{Binding Cheques}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="White"
                          BorderThickness="0"
                          IsReadOnly="True"
                          RowHeight="52"
                          SelectionMode="Single"
                          AlternatingRowBackground="#F9FAFB">
                    
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#EFF6FF"/>
                                    <Setter Property="Foreground" Value="#1E40AF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <!-- Type Icon & Label -->
                        <DataGridTemplateColumn Header="النوع" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding IsIncoming, Converter={StaticResource ChequeDirectionConverter}, ConverterParameter=Icon}" 
                                                   Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding IsIncoming, Converter={StaticResource ChequeDirectionConverter}}" 
                                                   FontSize="12" VerticalAlignment="Center" FontWeight="Medium"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="كود الشيك" Binding="{Binding Code}" Width="90"/>
                        <DataGridTextColumn Header="رقم الشيك" Binding="{Binding CheckNumber}" Width="100"/>
                        <DataGridTextColumn Header="الطرف الآخر" Binding="{Binding RelatedName}" Width="*"/>
                        <DataGridTextColumn Header="البنك" Binding="{Binding BankName}" Width="140"/>

                        <!-- Amount with Formatting -->
                        <DataGridTemplateColumn Header="المبلغ" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Amount, StringFormat='#,##0.00 EGP'}" 
                                               FontWeight="Bold" Foreground="#111827" HorizontalAlignment="Right"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Due Date with Overdue Highlight -->
                        <DataGridTemplateColumn Header="تاريخ الاستحقاق" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding DueDate, Converter={StaticResource OverdueToColorConverter}}" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="{Binding DueDate, StringFormat=dd/MM/yyyy}" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Status Badge -->
                        <DataGridTemplateColumn Header="الحالة" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Center"
                                            Background="{Binding Status, Converter={StaticResource ChequeStatusToColorConverter}}">
                                        <TextBlock Text="{Binding Status}" FontWeight="Bold" FontSize="11"
                                                   Foreground="{Binding Status, Converter={StaticResource ChequeStatusToColorConverter}, ConverterParameter=Foreground}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Actions Area - Status Updates Only -->
                        <DataGridTemplateColumn Header="تحديث الحالة" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✅" ToolTip="{Binding IsIncoming, Converter={StaticResource ChequeActionTooltipConverter}, ConverterParameter=Accept}" Style="{StaticResource ActionBtn}" Click="Accept_Click" Tag="{Binding}" FontSize="16"/>
                                        <Separator Width="1" Height="20" Background="#E5E7EB" Margin="6,0"/>
                                        <Button Content="❌" ToolTip="{Binding IsIncoming, Converter={StaticResource ChequeActionTooltipConverter}, ConverterParameter=Reject}" Style="{StaticResource ActionBtn}" Click="Reject_Click" Tag="{Binding}" FontSize="16"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</Page>
