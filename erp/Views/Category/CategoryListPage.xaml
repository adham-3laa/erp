<Page x:Class="erp.Views.Category.CategoryListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:erp.ViewModels.CategoryView"
      Background="{StaticResource PageBgBrush}"
      FlowDirection="RightToLeft">

    <Page.DataContext>
        <vm:CategoryListViewModel/>
    </Page.DataContext>

    <Page.Resources>

        <!-- ===== Glow ===== -->
        <DropShadowEffect x:Key="PrimaryPageGlow"
                          Color="{Binding Color, Source={StaticResource PrimaryBrush}}"
                          BlurRadius="26"
                          ShadowDepth="0"
                          Opacity="0.45"/>

        <DropShadowEffect x:Key="PrimaryOutlineGlow"
                          Color="{Binding Color, Source={StaticResource PrimaryBrush}}"
                          BlurRadius="16"
                          ShadowDepth="0"
                          Opacity="0.35"/>

        <!-- ===== Outer Table Border ===== -->
        <Style x:Key="GlowingTableOuterBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Effect" Value="{StaticResource PrimaryOutlineGlow}"/>
        </Style>

        <!-- ===== Column Header Style (BLACK) ===== -->
        <Style x:Key="CenteredHeaderStyle"
               TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource ProDataGridHeaderStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- ===== Cell Text Style (BLACK) ===== -->
        <Style x:Key="TopCenterCellTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- ===== DataGrid Cell ===== -->
        <Style x:Key="TopCenterGridCellStyle"
               TargetType="DataGridCell"
               BasedOn="{StaticResource ProDataGridCellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- ===== Delete Button ===== -->
        <Style x:Key="DeleteRedButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Background" Value="#B91C1C"/>
            <Setter Property="BorderBrush" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="10,6"
                                Effect="{StaticResource PrimaryOutlineGlow}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#991B1B"/>
                                <Setter Property="BorderBrush" Value="#DC2626"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#7F1D1D"/>
                                <Setter Property="BorderBrush" Value="#B91C1C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Margin="20">

        <Border CornerRadius="18"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource PrimaryBrush}"
                BorderThickness="1"
                Padding="16"
                Effect="{StaticResource PrimaryPageGlow}">

            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Headline -->
                        <RowDefinition Height="Auto"/>
                        <!-- Search + Buttons -->
                        <RowDefinition Height="Auto"/>
                        <!-- Error -->
                        <RowDefinition Height="*"/>
                        <!-- Table -->
                    </Grid.RowDefinitions>

                    <!-- ===== Headline (Centered) ===== -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="الأصــــــــــــنــــــــاف"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="Black"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Grid>

                    <!-- ===== Search + Buttons (Same Row) ===== -->
                    <Grid Grid.Row="1" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <!-- RTL: Column 0 appears on the right -->
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Search (Right side) -->
                        <StackPanel Grid.Column="0"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">

                            <TextBlock Text="بحث بالاسم :"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="ExtraBold"
                                       Margin="20,0,10,0"/>

                            <Border Style="{StaticResource GlowInputFrame}" Width="260">
                                <Border Style="{StaticResource GlowInputInner}">
                                    <TextBox Style="{StaticResource GlowTextBox}"
                                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                                </Border>
                            </Border>
                        </StackPanel>

                        <!-- Buttons (Left side) -->
                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Margin="150,0,0,0">

                            <Button Style="{StaticResource DarkButtonStyle}"
                                    Content="➕ إضافة"
                                    Margin="6,0"
                                    Click="Add_Click"/>

                            <Button Style="{StaticResource DarkButtonStyle}"
                                    Content="✏️ تعديل المحدد"
                                    Margin="6,0"
                                    Click="Edit_Click"/>

                            <Button Style="{StaticResource DarkButtonStyle}"
                                    Content="تحديث"
                                    Margin="6,0"
                                    Command="{Binding RefreshCommand}"/>

                            <Button Style="{StaticResource DeleteRedButtonStyle}"
                                    Content="حذف المحدد"
                                    Margin="6,0"
                                    Command="{Binding DeleteSelectedCommand}"/>
                        </StackPanel>
                    </Grid>

                    <!-- ===== Error ===== -->
                    <Border Grid.Row="2"
                            Margin="0,0,0,10"
                            CornerRadius="14"
                            Background="#FFFEE2E2"
                            BorderBrush="#FFFCA5A5"
                            BorderThickness="1"
                            Padding="12"
                            Visibility="{Binding Error, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
                        <DockPanel>
                            <TextBlock Text="⚠️" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Error}"
                                       Foreground="Black"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>

                    <!-- ===== Table ===== -->
                    <Border Grid.Row="3" Style="{StaticResource GlowingTableOuterBorder}">

                        <DataGrid Style="{StaticResource ProDataGridStyle}"
                                  ColumnHeaderStyle="{StaticResource CenteredHeaderStyle}"
                                  RowStyle="{StaticResource ProDataGridRowStyle}"
                                  CellStyle="{StaticResource TopCenterGridCellStyle}"
                                  ItemsSource="{Binding ItemsView}"
                                  SelectedItem="{Binding Selected}"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="None"
                                  Foreground="Black">

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="الـــــــــــكـــــــــــود"
                                                    Binding="{Binding Id}"
                                                    Width="2*"
                                                    ElementStyle="{StaticResource TopCenterCellTextStyle}"/>

                                <DataGridTextColumn Header="الاســــــــــــــــــــــم"
                                                    Binding="{Binding Name}"
                                                    Width="2*"
                                                    ElementStyle="{StaticResource TopCenterCellTextStyle}"/>

                                <DataGridTextColumn Header="الـــــــــــوصـــــــــــف"
                                                    Binding="{Binding Description}"
                                                    Width="3*"
                                                    ElementStyle="{StaticResource TopCenterCellTextStyle}"/>
                            </DataGrid.Columns>

                        </DataGrid>
                    </Border>

                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>
