<Page x:Class="erp.Views.Category.CategoryListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:erp.ViewModels.CategoryView"
      Background="{StaticResource PageBgBrush}"
      FlowDirection="RightToLeft">

    <Page.DataContext>
        <vm:CategoryListViewModel/>
    </Page.DataContext>

    <!-- UI-only resources (no logic changes) -->
    <Page.Resources>

        <!-- ====== GLOW / SHADOW (color of buttons) ====== -->
        <DropShadowEffect x:Key="PrimaryPageGlow"
                          Color="{Binding Color, Source={StaticResource PrimaryBrush}}"
                          BlurRadius="28"
                          ShadowDepth="0"
                          Opacity="0.55"/>

        <DropShadowEffect x:Key="PrimaryOutlineGlow"
                          Color="{Binding Color, Source={StaticResource PrimaryBrush}}"
                          BlurRadius="18"
                          ShadowDepth="0"
                          Opacity="0.45"/>

        <!-- ====== TABLE OUTER BORDER STYLE (border = button color + glow) ====== -->
        <Style x:Key="GlowingTableOuterBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Effect" Value="{StaticResource PrimaryOutlineGlow}"/>
        </Style>

        <!-- ====== Headers centered (FIXED: removed TextAlignment) ====== -->
        <Style x:Key="CenteredHeaderStyle"
               TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource ProDataGridHeaderStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- ====== Text inside cells: TOP + CENTER horizontally + BLACK ====== -->
        <Style x:Key="TopCenterCellTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- ====== Cell alignment: top + center (keeps your ProDataGridCellStyle look) ====== -->
        <Style x:Key="TopCenterGridCellStyle"
               TargetType="DataGridCell"
               BasedOn="{StaticResource ProDataGridCellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- ====== Red Delete button style based on your existing DarkButtonStyle ====== -->
        <Style x:Key="DeleteRedButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Background" Value="#B91C1C"/>
            <Setter Property="BorderBrush" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="10,6"
                                Effect="{StaticResource PrimaryOutlineGlow}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#991B1B"/>
                                <Setter Property="BorderBrush" Value="#DC2626"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#7F1D1D"/>
                                <Setter Property="BorderBrush" Value="#B91C1C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Margin="20">

        <!-- Page container: border + glow/shadow in button color -->
        <Border CornerRadius="18"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource PrimaryBrush}"
                BorderThickness="1"
                Padding="16"
                Effect="{StaticResource PrimaryPageGlow}">

            <!-- Inner Card -->
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <DockPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="الأصناف"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   DockPanel.Dock="Left"/>

                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                            <Button Style="{StaticResource DarkButtonStyle}"
                                    Content="➕ إضافة"
                                    Margin="6,0"
                                    Click="Add_Click"/>

                            <Button Style="{StaticResource DarkButtonStyle}"
                                    Content="✏️ تعديل المحدد"
                                    Margin="6,0"
                                    Click="Edit_Click"/>

                            <Button Style="{StaticResource DarkButtonStyle}"
                                    Content="تحديث"
                                    Margin="6,0"
                                    Command="{Binding RefreshCommand}"/>

                            <!-- Delete -> Red -->
                            <Button Style="{StaticResource DeleteRedButtonStyle}"
                                    Content="حذف المحدد"
                                    Margin="6,0"
                                    Command="{Binding DeleteSelectedCommand}"/>
                        </StackPanel>
                    </DockPanel>

                    <!-- Error -->
                    <Border Grid.Row="1"
                            Margin="0,0,0,10"
                            CornerRadius="14"
                            Background="#FFFEE2E2"
                            BorderBrush="#FFFCA5A5"
                            BorderThickness="1"
                            Padding="12"
                            Visibility="{Binding Error, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
                        <DockPanel>
                            <TextBlock Text="⚠️" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Error}"
                                       Foreground="#991B1B"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>
                        </DockPanel>
                    </Border>

                    <!-- Table outer border -->
                    <Border Grid.Row="2" Style="{StaticResource GlowingTableOuterBorder}">

                        <DataGrid Style="{StaticResource ProDataGridStyle}"
                                  ColumnHeaderStyle="{StaticResource CenteredHeaderStyle}"
                                  RowStyle="{StaticResource ProDataGridRowStyle}"
                                  CellStyle="{StaticResource TopCenterGridCellStyle}"
                                  ItemsSource="{Binding Items}"
                                  SelectedItem="{Binding Selected}"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="None"
                                  Foreground="Black">

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="الكود"
                                                    Binding="{Binding Id}"
                                                    Width="2*"
                                                    ElementStyle="{StaticResource TopCenterCellTextStyle}"/>

                                <DataGridTextColumn Header="الاسم"
                                                    Binding="{Binding Name}"
                                                    Width="2*"
                                                    ElementStyle="{StaticResource TopCenterCellTextStyle}"/>

                                <DataGridTextColumn Header="الوصف"
                                                    Binding="{Binding Description}"
                                                    Width="3*"
                                                    ElementStyle="{StaticResource TopCenterCellTextStyle}"/>
                            </DataGrid.Columns>

                        </DataGrid>
                    </Border>

                </Grid>
            </Border>

        </Border>
    </Grid>
</Page>
