<Page x:Class="EduGate.Views.Orders.CreateOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:EduGate.Views.Orders"
      Background="{StaticResource PageBgBrush}">

    <Grid Margin="20" MinWidth="720">
        <Grid.RowDefinitions>
            <!-- TopBar -->
            <RowDefinition Height="Auto"/>
            <!-- محتوى الصفحة -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ✅ TopBar ثابت -->
        <local:OrdersTopBar x:Name="OrdersTopBarControl"
                            Grid.Row="0"/>
        <!-- محتوى إنشاء الطلب -->
        <Border Grid.Row="1"
                Style="{StaticResource GlowGradientFrame}"
                HorizontalAlignment="Center"
                Width="720">

            <Border Style="{StaticResource InnerCardContainer}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Title -->
                        <RowDefinition Height="Auto"/>
                        <!-- عميل -->
                        <RowDefinition Height="Auto"/>
                        <!-- المنتج والكمية -->
                        <RowDefinition Height="Auto"/>
                        <!-- زر الحفظ -->
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <TextBlock Text="إنشاء طلب جديد"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,0,0,16"/>

                    <!-- اسم العميل -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,16">
                        <TextBlock Text="اسم العميل" Foreground="#6B7280" Margin="0,0,0,4"/>
                        <Border Style="{StaticResource GlowInputFrame}">
                            <Border Style="{StaticResource GlowInputInner}">
                                <TextBox x:Name="CustomerNameTextBox"
                                         Height="36"
                                         Padding="8"
                                         FontSize="14"
                                         VerticalContentAlignment="Center"
                                         Style="{StaticResource GlowTextBox}"/>
                            </Border>
                        </Border>
                    </StackPanel>

                    <!-- المنتج والكمية في صف واحد -->
                    <Grid Grid.Row="2" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>

                        <!-- المنتج -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="المنتج" Foreground="#6B7280" Margin="0,0,0,4"/>
                            <Border Style="{StaticResource GlowInputFrame}">
                                <Border Style="{StaticResource GlowInputInner}">
                                    <!-- ComboBox بمظهر مشابه لـ TextBox -->
                                    <Grid>
                                        <ComboBox x:Name="ProductComboBox"
                                                  Height="36"
                                                  Padding="8,0,28,0"
                                                  FontSize="14"
                                                  VerticalContentAlignment="Center"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  BorderBrush="Transparent"
                                                  Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Page}}"
                                                  ItemsSource="{Binding Products}"
                                                  DisplayMemberPath="Name"
                                                  SelectedValuePath="Id">

                                            <!-- تخصيص مظهر الـ ComboBox -->
                                            <ComboBox.Template>
                                                <ControlTemplate TargetType="ComboBox">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="20"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- النص المعروض -->
                                                        <ContentPresenter Grid.Column="0"
                                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                          Margin="8,0,0,0"
                                                                          VerticalAlignment="Center"
                                                                          HorizontalAlignment="Stretch"/>

                                                        <!-- سهم الـ ComboBox -->
                                                        <Path Grid.Column="1"
                                                              Data="M0,0 L4,4 8,0 Z"
                                                              Fill="#6B7280"
                                                              VerticalAlignment="Center"
                                                              HorizontalAlignment="Center"
                                                              Stretch="Uniform"/>

                                                        <!-- القائمة المنسدلة -->
                                                        <Popup x:Name="PART_Popup"
                                                               Grid.ColumnSpan="2"
                                                               AllowsTransparency="True"
                                                               Placement="Bottom"
                                                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                                               PopupAnimation="Slide">
                                                            <Border BorderThickness="1"
                                                                    BorderBrush="#E5E7EB"
                                                                    Background="White"
                                                                    CornerRadius="4">
                                                                <ScrollViewer>
                                                                    <ItemsPresenter/>
                                                                </ScrollViewer>
                                                            </Border>
                                                        </Popup>
                                                    </Grid>
                                                </ControlTemplate>
                                            </ComboBox.Template>

                                            <!-- تخصيص مظهر العناصر في القائمة -->
                                            <ComboBox.ItemContainerStyle>
                                                <Style TargetType="ComboBoxItem">
                                                    <Setter Property="Height" Value="36"/>
                                                    <Setter Property="Padding" Value="12,8"/>
                                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                    <Setter Property="Background" Value="White"/>
                                                    <Setter Property="Foreground" Value="#1F2937"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#F3F4F6"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter Property="Background" Value="#3B82F6"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ComboBox.ItemContainerStyle>
                                        </ComboBox>
                                    </Grid>
                                </Border>
                            </Border>
                        </StackPanel>

                        <!-- الكمية -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="الكمية" Foreground="#6B7280" Margin="0,0,0,4"/>
                            <Border Style="{StaticResource GlowInputFrame}">
                                <Border Style="{StaticResource GlowInputInner}">
                                    <TextBox x:Name="QuantityTextBox"
                                             Height="36"
                                             Padding="8"
                                             FontSize="14"
                                             VerticalContentAlignment="Center"
                                             Style="{StaticResource GlowTextBox}"
                                             PreviewTextInput="Quantity_PreviewTextInput"
                                             LostFocus="Quantity_LostFocus"
                                             HorizontalContentAlignment="Center"/>
                                </Border>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- زر الحفظ -->
                    <StackPanel Grid.Row="4"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,16,0,0">
                        <Button Content="تأكيد الطلب"
                                MinWidth="140"
                                Height="40"
                                FontSize="14"
                                Style="{StaticResource DarkButtonStyle}"
                                Click="ConfirmOrder_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>