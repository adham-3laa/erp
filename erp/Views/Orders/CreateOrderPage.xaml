<Page x:Class="erp.Views.Orders.CreateOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ordersViews="clr-namespace:erp.Views.Orders"
      Background="{StaticResource PageBgBrush}"
      FlowDirection="RightToLeft">

    <!-- ================= Resources ================= -->
    <Page.Resources>
        <DropShadowEffect x:Key="PrimaryOutlineGlow"
                          Color="{Binding Color, Source={StaticResource PrimaryBrush}}"
                          BlurRadius="16"
                          ShadowDepth="0"
                          Opacity="0.35"/>

        <Style x:Key="OrderCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect" Value="{StaticResource PrimaryOutlineGlow}"/>
        </Style>

        <Style x:Key="FieldTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="Margin" Value="0,14,0,6"/>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="OrderTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <!-- Border Style for TextBox -->
        <Style x:Key="TextBoxBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource SoftBorderBrush}"/>
        </Style>
    </Page.Resources>

    <!-- ================= Layout ================= -->
    <Grid Margin="24" MinWidth="720">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= TopBar ================= -->
        <ordersViews:OrdersTopBar x:Name="OrdersTopBarControl"
                                 Grid.Row="0"/>

        <!-- ================= Header ================= -->
        <TextBlock Grid.Row="1"
                   Text="إنشاء طلب جديد"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextDarkBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,16"/>

        <!-- ================= Form ================= -->
        <Border Grid.Row="2"
                Style="{StaticResource OrderCardStyle}"
                Width="700"
                HorizontalAlignment="Center">

            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">

                <StackPanel>

                    <!-- ===== Order Info ===== -->
                    <TextBlock Text="بيانات الطلب"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="اسم العميل" Style="{StaticResource FieldTitleStyle}"/>
                    <Grid>
                        <Border Style="{StaticResource TextBoxBorderStyle}">
                            <TextBox x:Name="CustomerNameTextBox"
                                     Style="{StaticResource OrderTextBoxStyle}"
                                     TextChanged="CustomerNameTextBox_TextChanged"
                                     GotFocus="CustomerNameTextBox_GotFocus"
                                     LostFocus="CustomerNameTextBox_LostFocus"
                                     KeyDown="CustomerNameTextBox_KeyDown"/>
                        </Border>

                        <!-- Suggestions Popup for Customer -->
                        <Border x:Name="CustomerSuggestionsBorder"
                                Background="White"
                                CornerRadius="8"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                Margin="0,42,0,0"
                                Visibility="Collapsed"
                                MaxHeight="200">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000"
                                                  BlurRadius="8"
                                                  ShadowDepth="2"
                                                  Opacity="0.3"/>
                            </Border.Effect>
                            <ListBox x:Name="CustomerSuggestionsListBox"
                                     BorderThickness="0"
                                     SelectionChanged="CustomerSuggestionsListBox_SelectionChanged"
                                     MouseLeftButtonUp="CustomerSuggestionsListBox_MouseLeftButtonUp"
                                     PreviewMouseDown="CustomerSuggestionsListBox_PreviewMouseDown">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Padding="10"
                                                   Cursor="Hand"
                                                   Text="{Binding}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Grid>

                    <TextBlock Text="اسم المندوب" Style="{StaticResource FieldTitleStyle}"/>
                    <Grid>
                        <Border Style="{StaticResource TextBoxBorderStyle}">
                            <TextBox x:Name="SalesRepNameTextBox"
                                     Style="{StaticResource OrderTextBoxStyle}"
                                     TextChanged="SalesRepNameTextBox_TextChanged"
                                     GotFocus="SalesRepNameTextBox_GotFocus"
                                     LostFocus="SalesRepNameTextBox_LostFocus"
                                     KeyDown="SalesRepNameTextBox_KeyDown"/>
                        </Border>

                        <!-- Suggestions Popup for Sales Rep -->
                        <Border x:Name="SalesRepSuggestionsBorder"
                                Background="White"
                                CornerRadius="8"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                Margin="0,42,0,0"
                                Visibility="Collapsed"
                                MaxHeight="200">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000"
                                                  BlurRadius="8"
                                                  ShadowDepth="2"
                                                  Opacity="0.3"/>
                            </Border.Effect>
                            <ListBox x:Name="SalesRepSuggestionsListBox"
                                     BorderThickness="0"
                                     SelectionChanged="SalesRepSuggestionsListBox_SelectionChanged"
                                     MouseLeftButtonUp="SalesRepSuggestionsListBox_MouseLeftButtonUp"
                                     PreviewMouseDown="SalesRepSuggestionsListBox_PreviewMouseDown">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Padding="10"
                                                   Cursor="Hand"
                                                   Text="{Binding}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Grid>

                    <TextBlock Text="نسبة العمولة (%)" Style="{StaticResource FieldTitleStyle}"/>
                    <Border Style="{StaticResource TextBoxBorderStyle}">
                        <TextBox x:Name="CommissionTextBox"
                                 Style="{StaticResource OrderTextBoxStyle}"
                                 FlowDirection="LeftToRight"
                                 PreviewTextInput="CommissionTextBox_PreviewTextInput"/>
                    </Border>

                    <!-- ===== Customer Number ===== -->
                    <TextBlock Text="رقم العميل" Style="{StaticResource FieldTitleStyle}"/>
                    <Border Style="{StaticResource TextBoxBorderStyle}">
                        <TextBox x:Name="CustomerNumberTextBox"
                                 Style="{StaticResource OrderTextBoxStyle}"/>
                    </Border>

                    <!-- ===== Products ===== -->
                    <TextBlock Text="المنتجات"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,24,0,10"/>

                    <DataGrid x:Name="ItemsGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              Height="220"
                              Margin="0,0,0,16">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="اسم المنتج"
                                                Binding="{Binding productname}"
                                                Width="*"/>

                            <DataGridTextColumn Header="الكمية"
                                                Binding="{Binding quantity}"
                                                Width="120"/>

                            <DataGridTemplateColumn Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="✕"
                                                Foreground="Red"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Click="RemoveItem_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- ===== Actions ===== -->
                    <DockPanel Margin="0,10,0,0">
                        <Button Content="+ إضافة منتج"
                                Width="140"
                                DockPanel.Dock="Left"
                                Click="AddItem_Click"/>

                        <Button Content="تأكيد الطلب"
                                Width="180"
                                Height="40"
                                DockPanel.Dock="Right"
                                Style="{StaticResource DarkButtonStyle}"
                                Click="ConfirmOrder_Click"/>
                    </DockPanel>

                </StackPanel>

            </ScrollViewer>
        </Border>
    </Grid>
</Page>
