<Page x:Class="erp.Views.Users.CurrentUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="600"
      d:DesignWidth="800">


    <Grid Background="Transparent">
        <!-- Loading Overlay -->
        <Border Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                Panel.ZIndex="100">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="6"/>
                <TextBlock Text="جاري تحميل بيانات المستخدم..." 
                           Margin="0,12,0,0"
                           Foreground="#6B7280"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
            <Grid>
                <!-- Back Button -->
                <Button Command="{Binding BackCommand}"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Width="40"
        Height="40"
        Margin="0,0,0,16"
        Background="#1E1B4B"
        Foreground="White"
        BorderThickness="0"
        Cursor="Hand"
        >
                    <materialDesign:PackIcon Kind="ArrowRight" Width="24" Height="24"/>
                </Button>

                <!-- Title -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,16">
                    <materialDesign:PackIcon Kind="AccountCircle" 
                                             Width="32" 
                                             Height="32"
                                             Foreground="#1E1B4B"
                                             HorizontalAlignment="Center"/>
                    <TextBlock Text="المستخدم الحالي"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="#111827"
                               HorizontalAlignment="Center"
                               Margin="0,8,0,0"/>
                </StackPanel>

                <!-- User Card -->
                <Border Background="White"
                        CornerRadius="16"
                        BorderThickness="1"
                        BorderBrush="#E8EAF2"
                        Padding="24"
                        Margin="0,60,0,16">
                    <Grid FlowDirection="LeftToRight">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Avatar -->
                        <Border Grid.Column="1"
                                Margin="580,47,40,303"
                                CornerRadius="50"
                                Background="#1E1B4B">
                            <TextBlock Text="{Binding Initials}"
                                       Foreground="White"
                                       FontSize="28"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>

                        <!-- User Info -->
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding CurrentUser.Fullname}"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="#111827"
                                       Margin="0,0,0,8"/>

                            <WrapPanel Margin="0,0,0,16">
                                <Border Background="{Binding UserStatusColor}"
                                        Padding="8,4"
                                        CornerRadius="8"
                                        Margin="0,0,12,0">
                                    <TextBlock Text="{Binding UserStatusText}"
                                               Foreground="White"
                                               FontWeight="Medium"
                                               FontSize="12"/>
                                </Border>

                                <Border Background="#1E1B4B"
                                        Padding="8,4"
                                        CornerRadius="8">
                                    <TextBlock Text="{Binding CurrentUser.UserType}"
                                               Foreground="White"
                                               FontWeight="Medium"
                                               FontSize="12"/>
                                </Border>
                            </WrapPanel>

                            <!-- Basic Info -->
                            <StackPanel Margin="0,0,0,16">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="Auto" MinWidth="504"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Username -->
                                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                                        <TextBlock Text="اسم المستخدم" FontSize="12" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding CurrentUser.Username}" 
                                                   FontSize="14" 
                                                   Foreground="#111827"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <!-- Email -->
                                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                                        <TextBlock Text="البريد الإلكتروني" FontSize="12" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding CurrentUser.Email}" 
                                                   FontSize="14" 
                                                   Foreground="#111827"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <!-- Phone -->
                                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                                        <TextBlock Text="رقم الهاتف" FontSize="12" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding CurrentUser.Phonenumber}" 
                                                   FontSize="14" 
                                                   Foreground="#111827"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <!-- Registration Date -->
                                    <StackPanel Grid.Row="1" Grid.Column="1">
                                        <TextBlock Text="تاريخ التسجيل" FontSize="12" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding FormattedDate}" 
                                                   FontSize="14" 
                                                   Foreground="#111827"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>

                            <!-- Action Buttons -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Width="628" Height="37">
                                <Button Command="{Binding ChangePasswordCommand}"
                                        Content="تغيير كلمة المرور"
                                        Background="#1E1B4B"
                                        Foreground="White"
                                        Padding="16,10"
                                        Margin="0,0,8,0"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        />

                                <Button Command="{Binding EditProfileCommand}"
                                        Content="تعديل الملف الشخصي"
                                        Background="#1E1B4B"
                                        Foreground="White"
                                        Padding="16,10"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
