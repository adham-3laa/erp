<Page x:Class="erp.Views.Dashboard.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:erp.ViewModels.Dashboard"
      Background="{StaticResource PageBgBrush}"
      FlowDirection="RightToLeft">

    <Page.DataContext>
        <vm:DashboardViewModel/>
    </Page.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Margin="0,0,0,12">
            <TextBlock Text="Dashboard"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"
                       DockPanel.Dock="Left"/>

            <Button Content="تحديث"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource DarkButtonStyle}"
                    DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,12">
            <Border Style="{StaticResource CardStyle}" Margin="6">
                <StackPanel>
                    <TextBlock Text="إجمالي مبيعات اليوم" Opacity="0.75"/>
                    <TextBlock Text="{Binding TotalSalesToday}" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Margin="6">
                <StackPanel>
                    <TextBlock Text="ربح اليوم" Opacity="0.75"/>
                    <TextBlock Text="{Binding TotalProfitToday}" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Margin="6">
                <StackPanel>
                    <TextBlock Text="طلبات Pending" Opacity="0.75"/>
                    <TextBlock Text="{Binding PendingOrdersCount}" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Margin="6">
                <StackPanel>
                    <TextBlock Text="طلبات Approved اليوم" Opacity="0.75"/>
                    <TextBlock Text="{Binding ApprovedOrdersCountToday}" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Low stock -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Low Stock Products"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"
                           Foreground="{StaticResource PrimaryBrush}"/>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding LowStockProducts}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding ProductName}"/>
                        <DataGridTextColumn Header="SKU" Binding="{Binding Sku}"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding CurrentQuantity}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>
