<Page x:Class="erp.Views.Payments.PayInvoiceByOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource PageBgBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= Title ================= -->
        <TextBlock Text="دفع فاتورة"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextDarkBrush}"
                   Margin="0,0,0,20"/>

        <!-- ================= Content ================= -->
        <Grid Grid.Row="1"
              HorizontalAlignment="Center"
              VerticalAlignment="Top">

            <Border Style="{StaticResource CardStyle}"
                    Padding="24"
                    Width="420">

<<<<<<< HEAD
                <StackPanel>
=======
                        <!-- Input Section -->
                        <TextBlock Text="أدخل المبلغ المراد دفعه" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,8" 
                                   Foreground="#374151"/>
                        
                        <TextBox Style="{StaticResource InputTextBox}" 
                                 Text="{Binding PaidAmountText, UpdateSourceTrigger=PropertyChanged}"/>
>>>>>>> ad1f622d97b67f8b3e45d4015a285558ad57c332

                    <!-- Remaining Amount -->
                    <TextBlock Text="المبلغ المتبقي"
                               FontWeight="SemiBold"
                               Margin="0,0,0,4"/>

                    <TextBlock Text="{Binding RemainingAmount}"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="DarkRed"
                               Margin="0,0,0,16"/>

                    <!-- Paid Amount -->
                    <TextBlock Text="المبلغ المراد دفعه"
                               FontWeight="SemiBold"/>

                    <TextBox Text="{Binding PaidAmount, UpdateSourceTrigger=PropertyChanged}"
                             Height="36"
                             Margin="0,6,0,20"/>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Left">

                        <!-- Pay -->
                        <Button Content="💰 تنفيذ الدفع"
                                Width="160"
                                Height="40"
                                Margin="0,0,12,0"
                                Command="{Binding PayCommand}"
                                Style="{StaticResource DarkButtonStyle}"/>

                        <!-- Back -->
                        <Button Content="⬅ رجوع"
                                Width="120"
                                Height="40"
                                Click="Back_Click"
                                Style="{StaticResource DarkButtonStyle}"/>

                    </StackPanel>



                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
